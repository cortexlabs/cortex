- kind: deployment
  name: dungeonmodel

- kind: api
  name: aidungeonmodel  # API name (required)
  endpoint: /aidungeon
  tensorflow:
    model: s3://cortex-examples/tensorflow/iris-classifier/nn
  compute:
    gpu: 1  # GPU request per replica (default: 0)
