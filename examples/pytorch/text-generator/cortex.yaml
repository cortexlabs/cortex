- kind: deployment
  name: text

- kind: api
  name: generator
  predictor:
    path: predictor.py
    config:
      num_words: 50
      device: cuda  # use "cpu" to run on CPUs
  compute:
    cpu: 1
    gpu: 1
