- kind: deployment
  name: answer

- kind: api
  name: generator
  predictor:
    path: predictor.py
    config:
      device: cuda  # use "cpu" to run on CPUs
  compute:
    cpu: 1
    gpu: 1
    mem: 4G
