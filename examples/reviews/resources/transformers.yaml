- kind: transformer
  name: tokenize_string_to_int
  output_type: INT_LIST_COLUMN
  inputs:
    columns:
      col: STRING_COLUMN
    args:
      max_len: INT
      stop_words: [STRING]
      vocab: {STRING: INT}
      reserved_indices: {STRING: INT}
