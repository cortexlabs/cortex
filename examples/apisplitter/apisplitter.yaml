# WARNING: you are on the master branch, please refer to the examples on the branch that matches your `cortex version`

# !!!!!
# before deploying the APISplitter the APIs "iris-classifier" and "another-iris-classifier" should be deployed.
# cortex deploy examples/sklearn/iris-classifier/cortex.yaml -e aws
# !!!!!
- name: iris-classifier-apisplitter
  kind: APISplitter
  networking:
    endpoint: iris-classifier-splitter
  apis:
    - name: iris-classifier
      weight: 20
    - name: another-iris-classifier
      weight: 80
      
