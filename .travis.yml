sudo: required

language: go
go: "1.11.5"

services:
  - docker

before_install:
  - sudo apt-get install -y apt-transport-https ca-certificates software-properties-common zip python3-pip python3-dev build-essential
  - sudo pip3 install --upgrade pip
  - pip3 install --upgrade awscli --user

jobs:
  include:
    - stage: base
      script: make tf-base
      name: "TF base images"
    - script: make spark-base
      name: "Spark base images"
    - stage: pipeline
      script: make tf-images
      name: "Tensorflow"
    - script: make spark-images
      name: "Spark"
    - script: make argo-images
      name: "Argo"
    - script: make operator-images
      name: "Operator"
    - stage: test
      script: make test
    - stage: deploy
      if: branch = master
      script: build-and-upload
      


